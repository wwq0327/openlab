from django.conf.urls.defaults import patterns, include, url

## urlpatterns = patterns('openlab.status',
##                        url(r'(?P<username>\w+)/home/st/(?P<id>\d+)/del/', 'st_del_page', name='st_del'),
##                        )

